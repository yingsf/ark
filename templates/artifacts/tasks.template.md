<!-- ark-artifact: tasks -->
<!-- schema-version: 1.1 -->
<!-- last-updated: YYYY-MM-DD -->

# Tasks

<!-- 本文件跟踪当前阶段的细粒度任务状态。由 /ark:ark-tasks 初始化，由 implement / debug / sync 小幅更新。-->
<!-- 不要在这里定义需求（那是 spec 的职责），也不要在这里描述阶段策略（那是 plan 的职责）。-->
<!-- Done = 已完成且已验证。未经验证的实现只能进入 Ready for validation，不应标记为 Done。-->

## 状态定义

| 状态 | 含义 |
|------|------|
| Todo | 尚未开始 |
| Doing | 当前正在进行 |
| Ready for validation | 实现已完成，等待 /ark:ark-validate 记录验证证据 |
| Done | 已完成且已验证（验证记录见 validation.md） |
| Blocked | 因依赖或外部条件无法继续，必须记录解除条件 |

## 任务格式

每条任务遵循以下结构：

```
- [ ] T{id} {任务描述}
  优先级：P0 / P1 / P2
  依赖：T{id}, T{id}（无依赖则省略）
  完成信号：什么情况下算这个任务完成（必填）
  完成后可观察结果：用户或维护者能看到什么变化
  真实性锚点：真实入口 / 真实依赖 / 真实数据源 / 公开契约 / 无直接锚点
  真实闭环推进：推进了什么真实入口 / 依赖 / 数据 / 契约；若无则说明原因
  预期验证等级：L0 / L1 / L2 / L3 / L4 / L5
  仍未覆盖：本任务完成后仍缺少的验证或真实条件
  下一步需要用户提供：无 / 配置 / 样例数据 / 凭据 / 审查意见 / 其他
  外部依据：无 / 官方文档 / 内部规范 / 历史项目 / 参考项目 / 静态分析报告 / 用户审查意见
  需吸收点：...（仅外部依据非“无”时）
  明确不吸收点：...（仅外部依据非“无”时）
  使用证据：...（仅外部依据非“无”且已使用时）
  当前批次：Batch n/m（{批次摘要}）（仅大型任务，无批次时省略）
  验证：validation.md #{章节}（Done 任务必填；Ready for validation 可写待验证方式）
  阻塞原因：...（仅 Blocked 任务）
  解除条件：...（仅 Blocked 任务）
```

## Done

<!-- 已完成且已验证的任务。验证字段必须指向 validation.md 中的对应记录。-->

## Doing

<!-- 当前正在进行的任务。建议同时不超过 2 条。-->

## Ready for validation

<!-- 实现已完成但尚未由 /ark:ark-validate 记录验证证据的任务。-->

## Todo

<!-- 待执行任务。按优先级排序。-->
<!-- 默认只展开当前执行窗口的 5-15 个任务；未来阶段用摘要，不要生成大量远期细碎任务。-->

## Blocked

<!-- 阻塞任务。阻塞原因和解除条件为必填项。-->

## Last updated
YYYY-MM-DD
